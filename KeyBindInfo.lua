-- Example table (delete later)
local module = {}

-- Example information
module["Keybinds"] = {
    -- Example keybind (delete later)
    DoSomething = {
        KeyCode = Enum.KeyCode.E, -- The key code for the action
        Toggle = false, -- If the keybind is a toggle or a hold (optional, not including means it's a single press)
    }

    -- Add more keybinds as needed
}